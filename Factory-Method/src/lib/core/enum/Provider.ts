/**
 * - En: Set the supported database types
 * - Es: Estable los tipos de base de datos soportados
 * @return {MySQL, PostgreSQL}
 */
export enum Provider {
  MySQL = 'mysql',
  PostgreSQL = 'postgres'
}
